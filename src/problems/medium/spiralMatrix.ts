import { ISolution } from "../../utils/ISolution";

export default class SpiralMatrix implements ISolution {
    spiralOrder(matrix: number[][]): number[] {
        return [];
    }

    solve() {
        console.log('howdy from the template');
    };
}